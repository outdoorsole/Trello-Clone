// (function() {
//   'use strict';

//   describe('ItemService', function() {
//     var service;

//     // Configure module that contains the controller being tested
//     beforeEach(module('mytodo'));

//     beforeEach(inject(function (_ItemService_) {
//       service = _ItemService_;
//     }));

//     it('should be 0', function() {
//       expect(service.getItem()).toEqual(0);
//     });

//     it('should set current list to 1', function() {
//       expect(service.setItem(1)).toEqual(1);
//       expect(service.getItem()).toEqual(1);
//     });
//   });
// })();
